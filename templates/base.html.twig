<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        {#===========0 el titulo se tiene que pasar por php y el favicon se tiene que cambiar =================#}
        <title>{% block title %}{% endblock %}</title>
        <link rel="favicon" href="{{ asset('res/icons/feather.svg')}}">

        <link rel="stylesheet" href="{{ asset('css/reset.css')}}"/>
        <link rel="stylesheet" href="{{ asset('css/base.css')}}"/>

        {% block stylesheets %} {#{ encore_entry_link_tags('app') }#}
        {% endblock %}

        <script src="{{ asset('js/base.js')}}"></script>

        {% block javascripts %} {#{ encore_entry_script_tags('app') }#}
        {% endblock %}
    </head>
    <body>
<!-- ===================================0
        barra lateral izquierda     -    tecnicamente el header-->
        <header>
            <div class="sidenav"> <!-- parte de arriba  https://feathericons.com/ -->
                <a href="{{ path('app_home') }}"><button><img id="home-btn" src="{{ asset('res/icons/home.svg') }}" alt="Home button"></button></a>
                <a href="{{ path('app_search') }}"><button><img id="search-btn" src="{{ asset('res/icons/search.svg') }}" alt="Likes button"></button></a>
                <a href="{{ path('app_chat') }}"><button><img id="chat-btn" src="{{ asset('res/icons/message-circle.svg') }}" alt="Star button"></button></a>
                <a href="{{ path('app_profile') }}" ><button><img id="user-btn" src="{{ asset('res/icons/user.svg') }}" alt="User profile picture"></button></a>
                <a href=""><button class="settings"><img id="settings-btn" src="{{ asset('res/icons/settings.svg') }}" alt="Setiings button"></button></a>
            </div>
        </header>
<!-- ===================================0
        cuerpo del texto    -->
        {% block body %}    {% endblock %}

    </body>
</html>